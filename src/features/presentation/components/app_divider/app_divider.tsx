import {Divider} from '@ui-kitten/components';
import React from 'react';

const AppDivider = () => {
  return (
    <Divider
      style={{
        backgroundColor: '#fff',
        height: 1,
        width: '100%',
        marginVertical: 10,
      }}
    />
  );
};

export default AppDivider;
